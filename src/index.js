/* eslint-disable no-unused-vars */     // eslint 关闭检查
import React from 'react'
import { render } from 'react-dom'
import App from './App'
/* eslint-enable no-unused-vars */     // eslint 开启检查

render((
  <App /> 
), document.getElementById('app')) 